# JVM build artifacts
bin/
build/
out/
.gradle/
*.class
*.log

# IDE / personal editor state
.classpath
.project
.settings/
.idea/
.vscode/
*.iml
*.geany
geany_run.bat

# Generated docs
javadoc/

# Game runtime data (local-only)
resources/logs/
resources/saves/
resources/save.txt

# Stale CWD-relative player state from the pre-AppPaths era (never ship these)
swing/resources/save.txt
swing/resources/settings.properties
swing/resources/achievements.txt

# Large binary assets — host these outside git
resources/music/*.mp3
resources/music/*.wav
resources/music/*.ogg
resources/music/*.m4a

# OS junk
.DS_Store
Thumbs.db

# Android local SDK path (never commit)
local.properties

# Machine-specific Gradle config. Untracked deliberately in 39788a3 — it pins a
# local JDK path, which breaks the build on every other machine and in CI.
gradle.properties

# Local agent/tool scratch
.claude/
_to_delete/
